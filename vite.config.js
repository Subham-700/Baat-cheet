import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    outDir: 'dist',  // Change this to 'public' if that's what you need
  },
  plugins: [
    // Add any Vite plugins you might need here
  ],
});
